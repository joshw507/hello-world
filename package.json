{
  "name": "new-report",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "cypress:pass": "npx cypress run --spec 'cypress/integration/pass/pass.spec.js' --browser chrome"
  },
  "dependencies": {
    "core-js": "^3.9.1",
    "nuxt": "^2.15.3"
  },
  "devDependencies": {
    "cypress": "^7.4.0"
  }
}
